__pycache__/
*.pyc
.pytest_cache/

# Runtime demo output - regenerated by running the app, not curated content
logs/*.jsonl

# Regenerable via ml/generate_synthetic_data.py + ml/build_training_set.py
# (see README) - kept out of git so the repo doesn't carry ~20MB of
# synthetic bootstrap data; models/vendor_uplift/*/  (the actual trained
# artifacts) ARE committed, this is only their raw training inputs.
ml/synthetic_logs/
ml/data/

local.settings.json
.env

venv